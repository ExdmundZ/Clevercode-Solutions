#include <iostream>
using namespace std;
int main()
{
int n = 0, sk = 0, sum = 0;
cin >> n;

for(int i = 0; i < n; i = i + 1)
{
    cin >> sk;
 if(sk%2!=0)
 {
     sum = sum + sk;
 }

 else
 {
  sum = sum + sk/2;
 }

}
cout << sum;
}
