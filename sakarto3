#include <iostream>
using namespace std;

int main()
{
    int a = 0, b = 0, c = 0, d = 0, e = 0, f = 0;
    cin >> a >> b >> c;
    if (a > c)
        swap(a, c);


    if (a > b)
        swap(a, b);



    if (b > c)
        swap(b, c);
    cout << a << ' ' << b << ' ' << c; 
    return 0;
}
